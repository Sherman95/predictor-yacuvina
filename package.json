{
  "name": "predictor-yacuvina-monorepo",
  "version": "1.0.0",
  "description": "Predictor de atardeceres para Yacuviña - Algoritmo 3.0",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "cd server && npm install",
    "build:client": "cd client && npm install && npm run build",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm run preview",
    "deploy:render": "echo 'Deploying to Render...'",
    "deploy:vercel": "cd client && vercel --prod",
    "compare-envs": "node compare-environments.js"
  },
  "keywords": [
    "weather",
    "prediction",
    "yacuvina",
    "ecuador",
    "sunset",
    "algorithm"
  ],
  "author": "Tu Nombre",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
